# リポジトリ全域レビュー（棚卸し＋リファクタ進捗）

調査日: 2026-08-06　最終更新: 2026-08-06  
方針: 計算式・REQUIREMENTS は変更しない。挙動不変の内部整理。  
関連: [TODO.md](../TODO.md) / [v1-review-findings.md](./v1-review-findings.md) / [ops/ARCHITECTURE.md](../ops/ARCHITECTURE.md)

---

## このファイルの読み方

| 見たいこと | どこを見るか |
|------------|--------------|
| **全体がどこまで終わったか** | 下の「全体サマリ」 |
| **各課題の状態（済／一部／未）** | 「発見一覧（ステータス付き）」 |
| **`app.js` 分割の残りと次の順番** | 「R1 分解ロードマップ」 |
| **次に人間が決めること** | 「次の合意ポイント」 |
| **フェーズ1で何をしたか** | 末尾「フェーズ1 実施ログ」 |

### ステータス記号（発見一覧・ロードマップ共通）

| 記号 | 意味 |
|------|------|
| **済** | 今回の目的どおり完了。追加作業不要 |
| **一部** | 着手済みだが、発見時の指摘はまだ残る |
| **未** | 未着手 |
| **後回し** | 意図的に今はやらない（理由あり） |
| **対象外** | 本リファクタのスコープ外（別タスク／やらない） |

---

## 全体サマリ（2026-08-06 時点）

| 区分 | 件数 | ID |
|------|------|-----|
| **済** | 9 | R2, R3, R5, L1, L2, L3, L5, L6, L7 |
| **一部** | 2 | R1, R4 |
| **未** | 3 | L4, L8, L9 |
| **後回し** | 0 | — |
| **対象外** | 0 | — |

**結論**: 致命バグなし。フェーズ1（低リスク整理）は完了。**フェーズ2（R1 続き）が現在の作業**。実機確認 2026-08-06 OK。

**規模感（更新）**

| 領域 | 行数（概算） |
|------|-------------|
| `app.js` | **~920**（分割前 ~2500） |
| `shareCardUi.js` | ~115（新規・順6） |
| `deckUi.js` | ~530 |
| `layoutMode.js` | ~130（新規・順5） |
| `eventUi.js` | ~400 |
| `resultTable.js` | ~140 |
| `scenarioLinkUi.js` | ~130 |
| `designMemoryUi.js` | ~175 |
| `designSessionUi.js` | ~195（新規・順3） |
| `app/css` 合計 | ~5400（境界整理済み・全面再監査は未） |

---

## 発見一覧（ステータス付き）

> **注意**: この表は「バグ一覧」ではなく「負債の棚卸し」。**済＝発見当時の指摘がすべて消えた**とは限らない（一部＝その例）。

### 致命

（なし）

### 中

| ID | 状態 | 箇所 | 内容 | フェーズ1での対応 |
|----|------|------|------|-------------------|
| **R1** | **一部** | `app.js` | God-object。イベント・結果表・レイアウト・メモリ等が同居 | `eventUi.js`・`resultTable.js` へ抽出。**残りはロードマップ参照** |
| **R2** | **済** | `style.css` ↔ `foundation.css` | 二重定義で cascade が読みにくい | 見た目=foundation / 配置=style に整理 |
| **R3** | **済** | 一度きりスクリプト | 誤実行で events 汚染のリスク | `scripts/archive/` へ隔離 |
| **R4** | **一部** | テスト | 継承 SP・合計の回帰が弱い | 継承 SP ユニット追加。**DOM E2E は未** |
| **R5** | **済** | merge / reclassify / `extract_mdb.py` | Runbook と npm の位置づけが曖昧 | `DATA.md` でレガシー明文化（削除はしない） |

### 軽

| ID | 状態 | 箇所 | 内容 | フェーズ1での対応 |
|----|------|------|------|-------------------|
| **L1** | **済** | `escapeHtml` | 二重実装 | `htmlEscape.js` に一本化 |
| **L2** | **済** | `characterBaseName` | 二重 export | `cardAssets.js` を正本に |
| **L3** | **済** | `renderSupportSlots` | 死コード | 削除 |
| **L4** | **未** | 版キャッシュバス | `?v=` / `DATA_CACHE_BUST` が散在 | 触っていない（版上げ手順のまま） |
| **L5** | **済** | 縦カード samples | v1/v2 試作残骸 | archive 化。npm から v3 削除 |
| **L6** | **済** | v1-review 鮮度 | 誤記・古い推奨順 | 訂正済み |
| **L7** | **済** | 完了 handoff の product 常駐 | archive 候補 | `docs/archive/` へ移動 |
| **L8** | **未** | spec 版表記 | 「v0.1.10 実機確認」等の古いラベル | 触っていない（事実は有効） |
| **L9** | **未** | v1-review L1–L6, L8 | 軽微バグ候補 | 構造リファクタとは別枠 |

---

## R1 分解ロードマップ（`app.js` 分割の残り）

**合意（2026-08-06）**: 選択肢 **B** — R1 の続きを実施。製品バグ修正（共有カード背景など）はリファクタ塊の合間または後。

### 済 — 抽出済みモジュール

| 塊 | ファイル | 行数目安 | 実機 |
|----|----------|----------|------|
| イベント UI（列下・ダイアログ・スプリット詳細） | `eventUi.js` | ~400 | OK（2026-08-06） |
| 結果表 tbody レンダ | `resultTable.js` | ~140 | OK（2026-08-06） |
| シナリオリンク・シニア RMJ チップ | `scenarioLinkUi.js` | ~130 | OK（2026-08-06） |
| 設計メモリダイアログ・一覧サムネ | `designMemoryUi.js` | ~175 | OK（2026-08-06） |
| セッション自動保存・スナップショット復元 | `designSessionUi.js` | ~195 | OK（2026-08-10） |
| 編成ダッシュボード・ピッカー・継承ポップオーバー | `deckUi.js` | ~530 | OK（2026-08-10） |
| ギャラリー⇔スプリット・合計バー DOM 移動 | `layoutMode.js` | ~130 | OK（2026-08-10） |
| 共有カードボタン（コピー／画像保存） | `shareCardUi.js` | ~115 | 実機確認待ち（2026-08-10 実装） |

### 未 — フェーズ2 で切り出す候補（推奨順）

| 順 | ID | 塊 | 想定ファイル | `app.js` 目安 | リスク | 推奨理由 |
|----|-----|-----|-------------|---------------|--------|----------|
| **1** | R1-b | シナリオリンク・シニア RMJ ラジオ | `scenarioLinkUi.js` | ~100行 | 低 | **済**（2026-08-06） |
| **2** | R1-c | 設計メモリダイアログ・一覧サムネ | `designMemoryUi.js` | ~120行 | 低〜中 | **済・実機 OK**（2026-08-06） |
| **3** | R1-d | セッション保存・復元・スナップショット配線 | `designSessionUi.js` | ~100行 | 中 | **済・実機 OK**（2026-08-10） |
| **4** | R1-e | 編成ダッシュボード・継承ポップオーバー・ピッカー配線 | `deckUi.js` | ~350行 | 中 | **済**（2026-08-10） |
| **5** | R1-f | ギャラリー⇔スプリット・modebar・合計バー移動 | `layoutMode.js` | ~120行 | **高** | **済・実機 OK**（2026-08-10） |
| **6** | R1-g | 共有カードボタン配線 | `shareCardUi.js` | ~80行 | 低 | **済**（2026-08-10） |

### `app.js` に残す想定（分割後も）

- `init` / `loadJson` / グローバル状態
- `recalc` オーケストレーション（`buildSkillPlan` 呼び出し〜合計更新）
- 各モジュールへの `createXxx(deps)` 配線

---

## 次の合意ポイント（フェーズ2）

**実装に入る前に、毎回ここだけ人間が OK する:**

1. 上表の **どの順番（1〜6）から着手するか**（推奨は 1→2→…→6）
2. **その塊だけ**やるか、複数まとめてやるか
3. 完了後の **実機確認範囲**（下記チェックリストから選ぶ）

### フェーズ2 実機チェックリスト（塊ごと）

- [ ] デフォルト編成・合計 SP（4281 前後）
- [ ] ギャラリー⇔スプリット（layout 塊のとき必須）
- [ ] サポカイベント選択（event 塊済・回帰用）
- [ ] 結果表 ON/OFF・ソート（result 塊済・回帰用）
- [ ] メモリ保存／復元（memory / session 塊のとき必須）
- [ ] シナリオリンク・RMJ 切替（scenario 塊のとき必須）
- [ ] 画像コピー／保存（share 塊のとき必須）

---

## やらない（リファクタ全体）

- フレームワーク／バンドラ導入
- 計算ルール再設計（`aggregate` 等の大再設計）
- モック HTML 一括削除
- L9 の個別バグ修正をリファクタと混ぜない（必要なら別 TODO）

---

## フェーズ1 実施ログ（2026-08-06・完了）

| 順 | 項目 | 結果 |
|----|------|------|
| 1 | docs／archive／死スクリプト隔離 | **済** |
| 2 | ユーティリティ一本化・死コード | **済** |
| 3 | CSS 責任境界 | **済** |
| 4 | `eventUi.js` / `resultTable.js` | **済** |
| 5 | 継承 SP テスト | **済** |
| 6 | レイアウト切替の分離 | **後回し** → フェーズ2 の R1-f へ |
| 7 | R5 レガシー明文化 | **済** |

コミット: `7cf8d4a` … `5e88c88`（v1.0.4 以降・**未 push** の可能性あり。push 前に `git status` 確認）

---

## 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-08-06 | 初版棚卸し |
| 2026-08-10 | 順6 `shareCardUi.js` 抽出 |
| 2026-08-10 | 順5 `layoutMode.js` 抽出・実機 OK |
| 2026-08-10 | 順4 `deckUi.js` 抽出・実機 OK |
| 2026-08-10 | 順3 `designSessionUi.js` 抽出・実機 OK |
| 2026-08-06 | 順2 `designMemoryUi.js` 抽出・実機 OK |
