# v1.0.0 向けレビュー調査（問題棚卸し）

調査日: 2026-08-02  
方針: **直さず一覧化**。修正は優先度付け後。  
関連: [TODO.md](../TODO.md) / [UX.md](./UX.md)（PC 主戦場・本格スマホは後段）

## 結論（1行）

コア SP 計算・金白／○◎・既存ユニットテストは健全。**M1（レギュ×チェーン）は 2026-08-02 案Aで修正済**。次は M3（手動 OFF）・M2（復元 ID）など。

## テスト現状

| スイート | 結果 |
|----------|------|
| `scripts/test_sp.mjs` | 通過 |
| `scripts/test_skill_activation.mjs` | 通過（コネクト／シンパシー チェーン含む） |
| `scripts/test_design_snapshot.mjs` | 通過 |
| `scripts/test_copy_included_skills.mjs` | 通過 |

未整備: 継承固有 SP の回帰、`recalc()` フルパスの統合。

## 重大度の定義

| 印 | 意味 |
|----|------|
| **致命** | 合計 SP や状態が常用フローで誤る／操作不能 |
| **中** | 特定条件で数値・状態がずれる。対人準備で踏まれうる |
| **軽** | 稀・表示のみ・仕様トレードオフ。v1.0.0 必須ではない |

---

## 発見一覧

### 致命

（なし）

### 中

| ID | 箇所 | 内容 | 状態 |
|----|------|------|------|
| ~~**M1**~~ | `getDisplayActivation` | レギュがチェーン下位で緩む | **修正済**（案A: 表示行 skillId のみ） |
| **M2** | `restoreDesign` | 復元で ID 未検証 | Open |
| **M3** | `pruneManualExclusions` | デッキ変更で手動 OFF 消失 | Open |
| **M4** | `scheduleSessionSave` | debounce で即閉じ欠落 | Open |

<details>
<summary>M1 旧記述（修正前）</summary>

金「コネクト」行に白「シンパシー」を OR 結合し、ダートレギュでも互換扱いになっていた。

</details>

### 軽

| ID | 箇所 | 内容 |
|----|------|------|
| L1 | `goldLower.js` | ×（`groupRate < 0`）がヒント経路に入ると cost 0 表示の余地（現行データでは実質未使用） |
| L2 | `aggregate.js` `nameToId` | 同名後勝ち。現行 events は `skillId` 付きのため実害なし |
| L3 | シナリオリンク不正 ID | 復元時に黙殺されリンクスキルが無言で消える |
| L4 | 除外チップ件数 | 手動＋レギュ合算。ラベル誤解の余地 |
| L5 | 結果ソート | スナップショット未保存。リロードで初期値 |
| L6 | 復元時サポ重複 | ピッカーは防ぐがスナップショット改ざんでは二重可 |
| L7 | テスト穴 | 継承 SP・`recalc` E2E・M1 の回帰なし |
| L8 | 画像コピー | clipboard 失敗時は `false` のみ（ユーザー向けメッセージは呼び出し側次第） |
| L9 | スマホ／狭幅 | UX どおり本格最適化は後段。v1 では最低限パス別タスク |

---

## 推奨修正順（次フェーズ）

1. ~~**M1**~~ — **完了**（2026-08-02）
2. **M3**（手動 OFF の行 ID 変化）— 受け入れを決める
3. **M2**（復元時 ID 検証＋警告）
4. **M4** — `beforeunload` / `visibilitychange` で flush を検討（必須度は低め）
5. **L7** — 上記修正とセットでテスト追加
6. スマホ最低限・公開準備はバグ取り後（[TODO.md](../TODO.md))

## PC 通しスモーク（調査用）

- [ ] デフォルト編成合計 ≈ 4281（`test_sp` と一致）  
- [ ] トレ Lv5→3・切れ者 ON/OFF で合計が動く  
- [ ] 金行手動 OFF → チェーン全体除外  
- [ ] 手動 OFF 後にレギュ変更 → 手動 OFF 維持  
- [ ] **コネクト＋ダートレギュ（M1）**  
- [ ] セッション: 変更→1秒待機→リロードで復元  
- [ ] メモリ保存／復元／サムネ  
- [ ] ギャラリー⇔スプリット切替後も modebar 操作可  
- [ ] 画像コピー／PNG（失敗時のフィードバック）  
- [ ] 使い方モーダル: 開閉で枠高が跳ねない  

---

## やらない（本調査の範囲外）

- 本格スマホ最適化  
- 大規模リファクタ・見た目の再デザイン  
- 30307/30308 U-tools 化（条件付き既存タスク）  
- 初 push（バグ取り・版上げ後）
