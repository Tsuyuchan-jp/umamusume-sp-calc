# 更新履歴

公開版の主な変更を記録する。細かい開発コミットは Git 履歴を参照。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.0.0/) に近いが、セマンティックバージョニングは緩め（`0.1.x` = 公開後の軽量改修）。

**運用**:
- 版上げ（リリース）を伴わない変更は **[Unreleased]** にためる（追加・変更・修正など、開発者／ユーザーに意味のある内容のみ）
- 次の版上げ時に Unreleased の内容をその版の節へ移し、Unreleased を空に戻す
- 作業キュー・後追いタスクは [TODO.md](./TODO.md) へ（CHANGELOG に TODO 節を置かない）

## [Unreleased]

## [1.0.6] — 2026-08-30

### 変更

- **ゲーム更新（8/24 パッチ）追従**: `master.mdb` 再 extract（skills 2153 / supports 553 / characters 264）
- **優先サポカ +2種**（計42種）: `[時に交わる海と空]ミスターシービー`（30312）、`[As if Guided]エフフォーリア`（30311）
- **イベント**: U-tools 未掲載のため GameWith 一時手載せ6件（30311/30312）。計117イベント
- **画像**: エピファネイア（114101）・新SSRサポカ2枚の webp 同梱
- **JS/CSS キャッシュバス**: `1.0.6`
- **実機確認済み**（2026-08-30）

## [1.0.5] — 2026-08-10

### 変更

- **内部構造**: `app.js` を機能別モジュールに分割（`eventUi` / `resultTable` / `scenarioLinkUi` / `designMemoryUi` / `designSessionUi` / `deckUi` / `layoutMode` / `shareCardUi`）。挙動・計算は不変
- **JS/CSS キャッシュバス**: `app.js` / `shareCard.js` / `cardPicker.js` / CSS に `?v=1.0.5`

### 追加

- **継承固有 SP 回帰**: `test_sp.mjs` に単価×個数の合計加算アサーション

## [1.0.4] — 2026-08-06

### 変更

- **結果 ONスキル数（広幅）**: 「結果」見出し行の右端に配置し、分数を大きく表示
- **JS/CSS キャッシュバス**: `app.js` / `shareCard.js` / CSS に `?v=1.0.4`

### 修正

- **サポカタイプ絞込チップ**: 色丸（○）クリックで選択が外れる問題を修正。dot の `data-type` をやめクラス色指定に変更し、クリック対象を `button.type-chip` に限定
- **結果枠スキル表**: ヘッダー行（ON / スキル）の背景が左端からずれて見える問題を修正（`.result-table-wrap` の左右 padding 除去）

## [1.0.3] — 2026-08-03

### 変更

- **画像保存 WebP 品質**: **0.95**（実機約 376KB。1.0 は約 1.6MB で不採用）
- **JS/CSS キャッシュバス**: `app.js` / `shareCard.js` / CSS に `?v=1.0.3`。Pages で古い保存ロジックが残るのを防ぐ

### 修正

- **WebP 判定**: `blob.type` 欠落時もマジックバイトで WebP を認識

## [1.0.2] — 2026-08-03

### 変更

- **共有カード保存**: PNG → **WebP**（品質 0.92・scale 2 維持）。ボタン文言「画像保存」。ファイル名 `{タイトル}-{YYYYMMDD}-{SP}sp.webp`。非対応ブラウザは JPEG/PNG へフォールバック
- **v1.0.1 取り消し**: 共有カード右背景・PNG保存導線の変更をロールバック（v1.0.0 相当に復帰）

### 修正

- **画像保存（Pages）**: 大きな共有カードで `canvas.toDataURL` がブラウザ上限に当たり DL できない問題を、Blob URL 経由の保存に変更して修正

## [1.0.0] — 2026-08-03

UX・デュアルレイアウト・編成スクショ・スマホ対応・レビュー修正（M1〜M4）を一通り閉じた **正式版**。GitHub Pages へ初 push（v0.1.x 以降のローカル積み上げを反映）。

### 追加

- **デュアルレイアウト**: ギャラリー（上段編成／下段結果）とスプリット（左編成・右結果）。広幅（≥1200px）は手動切替のみ。狭幅はギャラリー固定
- **編成スクショ共有**: 横長カード（v28.1）。画像コピー／PNG。広幅は modebar 右端、狭幅は結果見出し横
- **セッション自動復元** / **設計メモリ**（上限20・サムネ）／**編成タイトル**（共有・PNG名・メモリ連携の正本）／**初期状態リセット**
- **カード画像**: 優先サポカ40＋育成 `chr_icon` 全件同梱（`assets/`）。手順は [ops/ASSETS.md](./ops/ASSETS.md)
- **v1 レビュー棚卸し**: [product/v1-review-findings.md](./product/v1-review-findings.md)

### 変更

- **スマホ対応（≤1199px）**: modebar 非表示・共有は結果見出し・由来折り返し・合計バー2段・ツールバー1段・タップ領域・表狭幅・トレLv 枠内修正
- **編成タイトル**: 見出し風＋鉛筆編集。共有タイトル／PNGは `{タイトル}-{YYYYMMDD}-{SP}sp.png`。メモリ保存時に自動入力、復元時はメモリ名を反映
- **初期リセット**: 編成見出し右の循環アイコン＋「リセット」（確認ダイアログ。メモリ一覧は維持）
- **カードピッカー刷新**: R3＋R1（紫クロム・トグル絞込・選択バッジ）。育成アイコンは `race_dress_id` 正本、疑似カード除外
- **結果作業台**: レギュ即適用・4列表（ON／スキル／必要SP／由来）・ON 文言統一。手動 OFF はレギュ変更でも維持
- **イベント A/B/C**: 列下要約＋詳細（ギャラリーはダイアログ／スプリットは左ペイン）。サポカ自動は列下確認のみ。シナリオは足元帯
- **見た目**: 世界観背景・紫クロム・幾何タイル・Visual Foundation。使い方／README を現行UIに追従
- **「自動（幅）」削除**（旧 localStorage `auto` はギャラリー扱い）

### 修正

- **M1** レギュ×チェーン: 表示行 skillId の activation のみで互換判定
- **M2** 復元時 ID 検証（名簿照合・空欄化・警告）
- **M3** 手動 OFF のチェーン／グループ引き継ぎ
- **M4** セッション debounce flush（`visibilitychange` / `pagehide`）
- 使い方モーダルの常時表示／高さ跳ね・ピッカー keepId・スプリット切替操作不能・結果表スクロール など

### 確認

- `npm test` 緑
- スマホ対応・タイトル／リセット・M1〜M4 など **実機OK**（スマホ本体の広範確認は公開後）

## [0.1.14] — 2026-08-01

### 追加

- **設計メモリ**（ヘッダー「メモリ」）: 編成・前提（切れ者・トレLv・継承）・除外・レギュ絞込を名前付きで localStorage に保存／復元（上限20件）
- `designMemory.js` / `designSnapshot.js` の完全往復（options・excluded・committed filter）

### 修正

- サポカ／育成ウマ娘ピッカーでカード面が潰れて候補が見えない問題（`.card-face` に `aspect-ratio`）

### 変更

- バージョン表記を **v0.1.14**（`DATA_CACHE_BUST` 同値）

### 確認

- `npm test` 緑（スナップショット往復・メモリ CRUD）

## [0.1.13] — 2026-07-30

### 追加

- ゲーム更新追従（DMM `master.mdb`）: skills **2127** / supports **547** / characters **264**
- 優先サポカ **+2種**（計40種）: `[巻頭カラーの夏]キセキ`（30307）、`[夏空チルタイム]アーモンドアイ`（30308）
- 上記2枚のイベント **+6件**（計111・auto 90 / single 21）を `events.preserve.json` に **GameWith 一時手載せ**（[キセキ](https://gamewith.jp/uma-musume/article/show/570425) / [アーモンド](https://gamewith.jp/uma-musume/article/show/570424)）。置換手順は [TODO.md](./TODO.md)（30307 / 30308 の U-tools 化）参照
- 見送り: `[天才的ガチエモSUMMER!!]フサイチパンドラ`（30309）は今回優先枠に含めない

### 変更

- `extract:events` が U-tools 個別カードの取得失敗（404 等）でも全体を止めず続行するよう改善
- バージョン表記を **v0.1.13**（`DATA_CACHE_BUST` 同値）

### 確認

- ゲーム更新追従・優先40種・キセキ/夏空アーモンドのイベント計上 — **実機確認済み**（2026-07-30）

## [0.1.12] — 2026-07-23

### 修正

- GitHub Pages で古い `events.json` がブラウザキャッシュに残ると、優先枠の新カードが「イベント対応のみ」に出ずイベントも計上されない問題を回避（JSON fetch に版クエリ + `cache: "no-cache"`）

### 変更

- バージョン表記を **v0.1.12**

### 確認

- Pages 実機確認済み（2026-07-23）

## [0.1.11] — 2026-07-23

### 追加

- 優先サポカ **+1種**（計38種）: `[両手いっぱい、小倉愛]ナイスネイチャ`（id 30306）。イベント3件を U-tools+mdb 抽出で追加

### 変更

- 優先枠の新規追加方針を明文化: ガチ本育成で使う最新の**課金必須 SSR**を原則とし、過去 SR は網羅しない（無料配布 SSR は都度判断）
- バージョン表記を **v0.1.11**

### 確認

- ネイチャ小倉愛のイベント計上・優先38種表示 — **localhost 実機確認済み**（2026-07-23）。Pages 側はキャッシュ滞留の報告あり → v0.1.12 で対策

## [0.1.10] — 2026-07-17

### 変更

- 結果パネル **スキル数** に継承固有を個数分カウント（一覧は1行のまま。通常行は1行＝1本）。実機確認済み
- バージョン表記を **v0.1.10**

## [0.1.9] — 2026-07-16

### 追加

- オプション **トレヒントLv**（3 / 4 / 5、既定5）。編成サポカのトレヒントを一括指定。イベント・シナリオ由来は変更しない。実機確認済み

### 変更

- ツール内の「訓練ヒント」表記を **トレヒント** に統一
- オプション欄の並び（切れ者 → 継承固有 → トレヒントLv）とトレヒント行の1行レイアウト
- バージョン表記を **v0.1.9**

## [0.1.8] — 2026-07-15

### 追加

- 結果パネルに **含めるスキルをコピー** ボタン。含める ON の表示行スキル名をカンマ区切り1行でクリップボードへコピー（継承固有は対象外）。`copyIncludedSkills.js` と `test_copy_included_skills.mjs` を追加。実機確認済み

### 変更

- バージョン表記を **v0.1.8**

## [0.1.7] — 2026-07-14

### 変更

- 結果レギュ絞込に **適用ボタン**（セレクト変更は下書きのみ、適用で確定）。編成変更時は確定済み絞込を新規スキルに増分適用（手動「含める」は維持）。実機確認済み
- レギュ絞込 UI を枠でグルーピング（未適用時は枠色のみ強調）
- `skillActivation.js` に増分／全置換除外の pure 関数、`test_skill_activation.mjs` に回帰テスト追加
- バージョン表記を **v0.1.7**

## [0.1.6] — 2026-07-14

### 追加

- 結果スキル一覧に **発動条件タグ**（バ場・距離・作戦）と **レギュ絞込**（バ場／距離／作戦）。非互換スキルを合計から自動除外（手動「含める」は絞込変更でリセット）。実機確認済み
- `skills.json` の `activation` フィールド（mdb `skill_data` 条件から extract）
- `app/js/skillActivation.js`、`scripts/patch_skill_activation.mjs`、`scripts/test_skill_activation.mjs`

### 変更

- バージョン表記を **v0.1.6**
- 絞込 UI の「馬場」表記をゲーム内表記の **「バ場」** に統一
- 結果パネル見出し右に「スキル数 ON/全件」を表示（実機確認済み）

---

## [0.1.5] — 2026-07-14

### 追加

- ヘッダー右 **「使い方」** から開く **説明書モーダル**（ネイティブ `<dialog>`）。使い方・ヒントLvの算出・FAQ・制限と免責（GitHub / note / X 連絡先含む）。実機確認済み

### 変更

- バージョン表記を **v0.1.5**

---

## [0.1.4] — 2026-07-14

### 追加

- サポカフィルタ **「イベント対応のみ」**（デフォルト ON）。`events.json` の `prioritySupportIds`（37種）でセレクト候補を絞込
- 選択中カードはフィルタ外でもその枠に残す（既存の SSR／タイプ／検索と AND 結合）

### 変更

- バージョン表記を **v0.1.4**

---

## [0.1.3] — 2026-07-14

### 変更

- サポカイベント正本を **U-tools + mdb 抽出**に置換（Phase B 完了）
- `events.json` 102件（抽出100 + preserve 2）。旧 id は `events.id-aliases.json` に記録
- ゴールデン比較: 抽出100件すべて match（conflict 0）

### 追加

- `npm run apply:events` / `scripts/apply_extracted_events.mjs`
- `data/events.preserve.json`（たづなお出かけ/正月・実機確認済み）
- `data/events.default-overrides.json`（ドトウ連続・ヤング Dreams デフォルト）

### 削除（手メンテ誤記）

- グラン「成長のヒント」（U-tools に無し）
- フジ「スライハンド」auto 登録（U-tools ではヒント無し）

---

## [0.1.2] — 2026-07-13

### 追加

- 優先サポカ **+26種**（計37種・103イベント）。GameWith / U-tools 照合の手メンテ
- `scripts/merge_support_events.mjs`（調査JSON → `events.json` マージ＋skillId 解決）
- `scripts/generate-support-events-26.mjs` / `data/support-events-research-26.json`（調査正本）

### 変更

- `bind_priority.mjs` を `prioritySupportNames` から自動生成するよう汎用化
- サポカイベント注意書きの件数（11種 → 37種）
- バージョン表記を **v0.1.2**

### 修正

- アストンマーチャン「いつもそばから見つめてる」を削除（GameWith誤記の捕捉ヒント。実機・U-toolsではステータスのみ）

---

## [0.1.1] — 2026-07-13

### 追加

- ○→◎ のガチ想定合算（ヒントは○準拠、◎専用ヒントなし）
- グループ内チェーン合算（○+◎、○+◎+金）。内訳は `(78+84+135)` 形式
- サポカイベント「優先11種のみ」注意（見出し横ボタンのアコーディオン）
- `scripts/relink_skills.mjs`（`group_rate < 0` を除いたリンク再構築）
- 本ファイル（更新履歴）

### 修正

- `group_rate < 0` の × を購入チェーンから除外（extract・`skills.json`・実行時）
  - 例: 冬ウマ娘◎、弧線のプロフェッサーで3項合算になっていた問題

### 変更

- バージョン表記を **v0.1.1**

---

## [0.1.0] — 2026-07-13

### 追加

- **GitHub Pages 公開**（https://Tsuyuchan-jp.github.io/umamusume-sp-calc/app/）
- ヘッダーにバージョン表記
- 公開向け README 整理（利用者向けを先頭、開発手順は `docs/ops/`）
- デプロイ workflow（`verify` / `test` 後に Pages 配信）
- ルート `index.html` から `/app/` へリダイレクト

### 修正

- 公開前レビュー High/Medium（R01–R10）

---

## [0.1] — 2026-07（タグ `v0.1`）

### 追加

- 画面下部の合計 SP 固定バーと差分ハイライト

---

## 0.1.0 以前（開発・未公開）

トレセン軒シナリオ、リンク白/金、RMJ 自動計上、優先11サポカイベント、金+白コスト合算などのコア機能を実装。詳細は当時の ROADMAP Phase A–C（現在は [CHANGELOG](./CHANGELOG.md) / [TODO](./TODO.md)）。
